# Models

Place the MediaPipe Tasks Pose Landmarker model here:

```plain text
pose_landmarker.task
```

The `.task` model file is intentionally not committed to this repository.

Download a compatible MediaPipe Pose Landmarker `.task` model and place it in this directory before running the recorder, or pass a custom model path with `--model`.
